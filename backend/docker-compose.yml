version: '3.8'
services:
  db:
    image: postgres:18
    container_name: betrillionaire_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: kral123
      POSTGRES_DB: bet
    ports:
      - '5433:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: